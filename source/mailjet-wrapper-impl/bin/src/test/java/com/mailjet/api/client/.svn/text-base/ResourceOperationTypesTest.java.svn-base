package com.mailjet.api.client;

import org.junit.Assert;
import org.junit.Test;

import com.mailjet.api.client.model.ResourceOperationTypes;

@SuppressWarnings("javadoc")
public class ResourceOperationTypesTest {
	
	@Test
	public final void testCreation(){
		Assert.assertEquals(ResourceOperationTypes.Get, ResourceOperationTypes.valueOf("Get"));
		Assert.assertEquals("You added a new operation type! Fix conditionals based on enum values.", 5, ResourceOperationTypes.values().length);
	}
}